# Redis知识点

## 常用命令

## 配置文件

配置文件是给redis服务器用的

## 数据持久化

数据持久化redis已经做好了，我们要做的就是改配置文件

## hiredis的使用

就相当于redis的api，不同语言有不同的redis api ,网址:...


## 其他

nginx采用边缘触发（反向代理），redis采用水平触发（不用一次全处理，下次会继续触发）

服务器端redis（redis是支持键值对的共享内存），客户端共享内存