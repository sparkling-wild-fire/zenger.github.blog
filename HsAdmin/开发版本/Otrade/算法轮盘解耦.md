# 算法轮盘解耦

3、 （赵泽杰）从O32订阅接收其他分片的算法委托并展示到界面
（1） 根据开启的产品过滤所需委托（通过序号号段过滤），在外部委托处理流程里需要把子单号落进宽途的委托表里
（因为需要展示其他分片的方案的子单号），但是无需落算法子单表。

1. 通过序号号段过滤：啥东西？所有分片的分片号集合吗？

（2） 做市：调用委托服务时不传递方案号、子单号，传输的宽途的委托号entrust_no到投资系统的l_extsystem_id字段，
回报时根据委托号号段+子单号不存在+来源来区分内部委托和外部委托。本次改造因为需要接收其他分片的算法委托并展示到界面上，
宽途的方案（extsystem_id为2）也需要传递子单号，为兼容之前的老做市分片（extsystem_id也为2），老做市主要是非指令业务，
做算法的基本是指令业务，所以extsystem_id为2, ins_code为0不传输子单号，其他场景需要传输子单号。
（单分片场景虽然调用O32服务的时候宽途没有传入子单号，但是本地是落入子单委托表和宽途委托表里的子单号字段的，界面可以展示出来）。
后续如果做市要做指令模式，需要升级支持到这次改造的版本，进行方案号号段分）

1. 传输的宽途的委托号entrust_no到投资系统的l_extsystem_id字段：为啥？
2. `回报`:是啥意思
2. `委托号号段+子单号不存在+来源`怎么区分内存委托和外部委托

（3） 由于没有子单表，宽途这边的业务逻辑不得访问子单表，440240023-LS_宽驰极速_委托查询里scheme_code不能从子单表获取，
改成解析从子单号再关联方案表查找（查找不到不报错），宽途的委托消息回报消息主推里LF_UFT公共_根据算法子单号查询方案代码,
改从方案表获取scheme_code。


4、支持新建传入是否过风控

1. 是只要前台传了参数就行了吗？



1. 委托下达已经落宽途委托表了，确认还要落啥？
2. 委托服务，本来也没有落算法子单表啊，是说内部委托要落？不对啊，内部委托保持原有逻辑就行了