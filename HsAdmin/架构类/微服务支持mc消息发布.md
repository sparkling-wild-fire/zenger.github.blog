# 微服务支持mc消息发布

1. mc声明一个主题，一般来说，workspace下面的topics.xml里配置就行
    - 不能在workspace下面配置，要在zk上注册的mc配置文件里去配置（这个mc是要和OT后台分离的吧？）
    - mc在注册的时候，会将mc上的功能号注册到zk上吗，怎么看啊！会的话我就将mc消息直接路由到微服务插件就行了
2. 通过微服务插件的消息发送接口，发送这个主题的消息到mc
   - 怎么让微服务插件的mc_client去发送？
   - 不知道哪个功能号是去调这个消息的，路由怎么配置，通过branch_no吗？
     - PubMsgByPacker函数的作用我就理解为，调用一个620开头的不确定的功能号，入参是topic_name,msg_body等，
     - 不行，应该不能通过路由的方式，否则你路由怎么配啊，那么mc是总线模式下才能路由，微服务模式不能路由啊


1. mc注册到zk上，mc新增我的主题
2. 试一下直接路由到微服务插件，mc能抓到功能号吗，就是没有入参而已